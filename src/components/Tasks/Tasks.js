import React from 'react';
import './Tasks.css';
const Tasks = (props) => {
    console.log(props)
    return (
        <div className="task-container">
            <h2>Tasks</h2>
        </div>
    );
};

export default Tasks;